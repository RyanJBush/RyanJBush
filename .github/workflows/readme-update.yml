name: README Validation

on:
  push:
    branches:
      - main

jobs:
  validate-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate README exists and is non-empty
        run: |
          test -s README.md
          echo "README.md validation passed."
